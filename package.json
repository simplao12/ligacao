{
  "name": "stvbr-vercel",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Portal de cadastro IPTV + API de pareamento seguro — pronto para deploy no Vercel.",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@vercel/postgres": "^0.8.0",
    "@vercel/kv": "^3.0.0"
  },
  "scripts": {
    "setup-db": "node scripts/setup-db.js"
  }
}
