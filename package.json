{
  "name": "stvbr-vercel",
  "version": "1.0.0",
  "private": true,
  "description": "Portal de cadastro IPTV + API de pareamento — pronto para deploy no Vercel.",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@vercel/kv": "^3.0.0"
  }
}
